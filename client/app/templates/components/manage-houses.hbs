
<div class="ui purple segment">
  <div class="ui list">
  {{#liquid-unless model}}
    No houses!
  {{/liquid-unless}}
  {{#each model as |house|}}
    <div class="item">
      {{house.name}} | <i class="edit icon" {{action 'editHouse' house}}></i>
    </div>
  {{/each}}
  </div>
</div>
